name: Deploy Laravel Project on push
on:
  push:
    branches:
      - main
jobs:
  web-deploy:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
      - name: Get the latest code
        uses: actions/checkout@v2.3.2
      - uses: actions/setup-node@master
      - name: 📂 Sync files
        uses: SamKirkland/FTP-Deploy-Action@4.0.0
        with:
          server: ${{ secrets.CPANEL_FTP_SERVER }}  50.63.177.140
          username: ${{ secrets.CPANEL_FTP_USERNAME }} newbarkat1@rorbits.com
          password: ${{ secrets.CPANEL_FTP_PASSWORD }} UtQx,_bqU=xt
          server-dir: /
